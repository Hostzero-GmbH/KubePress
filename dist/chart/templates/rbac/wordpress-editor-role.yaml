{{- if .Values.rbacHelpers.enable }}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
    labels:
        app.kubernetes.io/managed-by: {{ .Release.Service }}
        app.kubernetes.io/name: {{ include "kubepress.name" . }}
        helm.sh/chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
        app.kubernetes.io/instance: {{ .Release.Name }}
    name: {{ include "kubepress.resourceName" (dict "suffix" "wordpress-editor-role" "context" $) }}
rules:
    - apiGroups:
        - crm.hostzero.de
      resources:
        - wordpresses
      verbs:
        - create
        - delete
        - get
        - list
        - patch
        - update
        - watch
    - apiGroups:
        - crm.hostzero.de
      resources:
        - wordpresses/status
      verbs:
        - get
{{- end }}
